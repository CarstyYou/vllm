# 母 plan 指针

本任务的母 plan（目标 / 决策 / sub-task 总表 / risks）在父 repo：
`$MEGA_ROOT/.claude/work_plans/e2e_precision_plan.md`（`.claude/current_plan` 指向它）。

本目录只放 per-task 的 `task_NN/`（plan/tests/results）与 `memory/findings.md`，
结构见 `../rules/workflow.md`。
